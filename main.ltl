// ltl-dtw/main.ltl
//
// Dynamic time-warping for time-series similarity.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-dtw v0.1.0")
    io::println("Dynamic time-warping for time-series similarity.")
    0
}
