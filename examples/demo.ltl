// examples/demo.ltl — runnable demo for ltl_dtw

import std::io
use ltl_dtw as lib

fn main() {
    io::println("ltl_dtw demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}